# Keep editor/agent context files in-tree for local development, but exclude
# them from the `git archive` source release tarball. AGENTS.md is the canonical
# (vendor-neutral) file and ships in the release; GEMINI.md is a symlink to it
# and is tool-specific clutter in an official ASF source release. Claude Code
# reads AGENTS.md directly when no CLAUDE.md exists, so no CLAUDE.md is kept.
GEMINI.md export-ignore
